- name: "Installing Nginx webserver & Run the service"
  hosts: web
  become: yes
  tasks:
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Run nginx
    ansible.buitin.service:
      name: nginx
      state: started
    